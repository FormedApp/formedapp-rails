<h1>New Track</h1>

<% if @track.errors.any? %>
  <div>
    <h2><%= pluralize(@track.errors.count, "error") %> prohibited this edit from being saved:</h2>
    <ul>
      <% @track.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
<% end %>

<%= form_for(@track) do |f| %>
  <div class="form-group">
    <label>Title</label>
    <%= f.text_field :title, class: 'form-control' %>
  </div>

  <div>
    <%= f.submit 'Save', class: 'btn btn-primary' %>
  </div>
<% end %>

